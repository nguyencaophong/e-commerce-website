<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_store_item_management"
    android:gravity="center"
    android:orientation="vertical"
    android:visibility="visible">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="48dp">

        <ImageView
            android:id="@+id/ivStoreManagementSrcImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="#959595"
            android:src="@drawable/ic_trash_can"
            app:tint="@color/black" />
    </FrameLayout>

    <TextView
        android:id="@+id/tvStoreManagementItemTitle"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_store_management_item_title"
        android:gravity="center"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textColor="#ffffff"
        android:textSize="18dp"
        android:textStyle="bold"
        android:visibility="visible" />
</LinearLayout>
